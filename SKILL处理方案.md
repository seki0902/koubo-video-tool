# Skill 处理方案

> 用途：新开窗口后，直接从这份文档继续讨论和开发 Koubo rewrite skill 的接入方式。

## 一句话结论

这个 skill 可以放进工具里，但不能当成“把整个仓库所有 Markdown 拼成一个超长提示词”来处理。

更稳的方式是：

```text
读取 SKILL.md 的总规则
+ 固定读取 docs/ 下的核心规则
+ 根据当前话题只选择 1 个最相关的 library 文件
+ 必要时少量读取 examples
+ 生成口播稿
```

## 当前问题

现在工具里的 skill 读取方式比较粗：

- 本地目录：只读取子目录里的 `.md` 文件；
- GitHub：下载 Markdown 后也按同样方式拼接；
- 根目录的 `SKILL.md` 可能被忽略；
- `docs/`、`examples/`、`library/` 容易被全部拼在一起；
- 没有按照用户输入的话题，只选择一个匹配的案例库文件；
- 没有区分 `STANDALONE.md` 这种适合人工/CLI 阅读的说明和 API 调用时应该使用的提示词。

这会导致几个问题：

- prompt 太长；
- 规则互相干扰；
- 示例和执行说明混在一起；
- 输出质量不稳定；
- 看起来像“skill 很复杂导致不好接入”，但本质是加载方式不对。

## 应该怎么接

### 第一阶段：按仓库原规则加载

先不要做复杂系统，先把现有 skill 的规则读对。

加载顺序建议：

```text
1. 读取根目录 SKILL.md
2. 固定读取 docs/*.md
3. 根据用户主题匹配 library/*.md 中的一个文件
4. 可选读取 examples/input-sample.md 和 examples/output-sample.md
5. 拼成最终 system prompt
```

当前仓库：

```text
https://github.com/seki0902/Koubo-rewrite-skill
```

仓库结构大致是：

```text
SKILL.md
STANDALONE.md
docs/
examples/
library/
```

这里最关键的是：`library/` 不应该每次全读。

### 第二阶段：做一个简单主题选择器

现在案例分类数量不多，不需要一开始就上向量数据库。

可以先做关键词匹配：

```text
单企业直招
实操攻略
平台工具
招聘会活动
认知打破
赛道红利
```

比如：

- 用户话题里出现“公司、岗位、内推、直招”，优先选 `单企业直招.md`；
- 出现“怎么做、步骤、教程、方法”，优先选 `实操攻略.md`；
- 出现“平台、工具、网站、系统”，优先选 `平台工具.md`；
- 出现“招聘会、活动、宣讲会”，优先选 `招聘会活动.md`；
- 出现“误区、真相、别再、很多人以为”，优先选 `认知打破.md`；
- 出现“机会、风口、红利、趋势”，优先选 `赛道红利.md`。

如果匹配不到，就用默认分类，比如 `实操攻略.md`，同时记录日志，方便之后优化。

### 第三阶段：把 skill 编译成结构化文件

等第一阶段跑稳后，可以做一次“编译”。

目标不是让模型更神秘，而是让工具更稳定：

```text
compiled_skill.json
```

里面可以放：

```json
{
  "core_rules": [],
  "style_rules": [],
  "risk_rules": [],
  "libraries": [
    {
      "name": "实操攻略",
      "keywords": ["步骤", "教程", "方法"],
      "content": "..."
    }
  ],
  "examples": []
}
```

这样工具每次生成时不用重新理解整个文件夹，只需要按规则取对应模块。

## CLI 要不要接

不建议把 Codex CLI 当成主链路。

原因很现实：

- CLI 依赖本机安装和登录；
- 商业化时客户环境不可控；
- 非交互调用、超时、错误解析、并发都要额外处理；
- 作为产品后端不够稳定；
- 出问题时很难给普通客户解释。

CLI 可以作为内部辅助：

- 帮你整理 skill；
- 把很多案例归类；
- 生成 `compiled_skill.json`；
- 做离线检查和更新。

但正式工具主流程建议走：

```text
工具后端
-> LLM API 或本地模型服务
-> 按规则加载 skill
-> 输出口播稿
-> 调蝉镜生成视频
```

## 如果实例很多怎么办

有很多实例不是坏事，反而是壁垒。

但处理方式要分阶段：

```text
少量案例：按文件夹分类 + 关键词匹配
几十到一两百条：给案例加标签
几百条以上：再考虑语义检索 / 向量库
```

现在不要一上来做复杂检索系统。先把已有 6 个分类文件用对，比盲目加技术更重要。

## 推荐落地顺序

1. 改 `skill` 加载器，支持读取根目录 `SKILL.md`。
2. 固定读取 `docs/*.md`。
3. 增加 `library` 选择逻辑，每次只加载一个最相关文件。
4. 保留 examples 为可选项，不默认全量塞入。
5. 设置页支持填写 GitHub 仓库根地址，并自动识别 `/projects` 这类误填链接。
6. 日志里显示本次实际加载了哪些 skill 文件。
7. 跑通后再考虑 `compiled_skill.json`。

## 技术判断

这件事不是“很难”，难点主要在边界处理：

- GitHub 地址可能填错；
- 本地 skill 路径可能不是仓库根目录；
- Markdown 文件编码和中文路径要稳；
- prompt 不能无限膨胀；
- 不同案例库不能互相污染；
- 用户需要知道这次到底用了哪个 skill 模块。

所以正确方向不是把工具做得更复杂，而是把 skill 变成一个可控的加载流程。

## 新窗口继续时可以直接做的事

下一步可以直接改代码：

```text
目标：重写 skill.BuildPrompt

输入：
- cfg.Skill.LocalPath
- cfg.Skill.GithubURL
- 用户输入的话题/原稿

输出：
- 最终 prompt
- 本次加载文件列表
```

需要重点看的文件：

```text
skill/skill.go
skill/skill_test.go
handlers/handlers.go
frontend/setup.html
```

推荐先做本地目录版本，测试通过后再补 GitHub 版本。
